//function creates object with properties of: type, height, and output
//returns object
export const createSoybean = () => {
    const seed = { 
        type: "Soybean",
        height: 20,
        output: 4
    }
    return seed
}