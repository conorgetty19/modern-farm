//function creates array with two objects having properties of: type, height, and output
//returns array
export const createCorn = () => {
    const seed = [{
        type: "Corn",
        height: 180,
        output: 6
    }, {
        type: "Corn",
        height: 180,
        output: 6
    }]

    return seed
}