//function creates object with properties of: type, height, and output
//returns object
export const createWheat = () => {
    const seed = { 
        type: "Wheat",
        height: 230,
        output: 6
    }
    return seed
}