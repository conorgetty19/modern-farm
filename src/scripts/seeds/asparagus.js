//function creates object with properties of: type, height, and output
//returns object
export const createAsparagus = () => {
    const seed = { 
        type: "Asparagus",
        height: 24,
        output: 4
    }
    return seed
}