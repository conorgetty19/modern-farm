//function creates object with properties of: type, height, and output
//returns object
export const createPotato = () => {
   const seed = { 
        type: "Potato",
        height: 32,
        output: 2
    }

    return seed
}